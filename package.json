{
	"dependencies": {
		"better-sqlite3": "^11.10.0",
		"discord.js": "14.16"
	},
	"scripts": {
		"start": "node index.js",
		"linecount": "find . -type f -name '*.js' -not -path './node_modules/*' -exec wc -l {} + | sort -n -r | head -n 10",
		"test": "echo \"No tests specified\" && exit 0"
	},
	"devDependencies": {
		"@types/better-sqlite3": "^7.6.13",
		"@types/node": "^22.13.1"
	}
}